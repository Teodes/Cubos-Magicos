function Loading() {
  return (
    <div className="App-header">
      <img
        src={"/images/logo/logo.png"}
        className="App-logo"
        style={{ height: "15rem" }}
        alt="logo"
      />
      <span>Cargando...</span>
    </div>
  );
}

export default Loading;
